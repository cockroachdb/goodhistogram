# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Rust crate build artifacts. Cargo.lock is intentionally not committed since
# goodhistogram is a library.
/rust/target/
/rust/Cargo.lock
